<%= form_with(model: gear) do |form| %>
  <% if gear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gear.errors.count, "error") %> prohibited this gear from being saved:</h2>

      <ul>
        <% gear.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
